#include "../dpcp_cmn.h"

/* Provide the implementation of below APIs in dpcp_cmn.cpp */
struct rte_mbuf;

__attribute__((weak))
void 
dp_punt_pkt_to_cp(dp_ctx_t *dp_ctx, struct rte_mbuf *mbuf) {

    /* FAKE */
}